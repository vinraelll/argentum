import { Text, View } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

export default function Settings() {
  return (
    <SafeAreaView>
      <View>
        <Text className="text-text-primary">Settings Page</Text>
      </View>
    </SafeAreaView>
  )
}

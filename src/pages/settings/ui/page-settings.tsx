import { AScreen } from "@shared/ui/a-screen"
import { Text, View } from "react-native"

export default function SettingsPage() {
  return (
    <AScreen>
      <View>
        <Text className="text-sm font-bold text-text-primary">Settings Page</Text>
      </View>
    </AScreen>
  )
}
